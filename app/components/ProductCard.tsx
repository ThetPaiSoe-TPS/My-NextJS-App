import React from 'react'

const ProductCard = () => {
  return (
    <div>
      <button onClick={()=> console.log("Add to Cart")
      }>Add to Cart</button>
    </div>
  )
}

export default ProductCard
